export enum CarStatus {
    WAREHOUSE = 'WAREHOUSE', // Склад (видно только владельцу)
    LISTED = 'LISTED', // Выставлен на продажу (публично в каталоге)
    ARCHIVED = 'ARCHIVED', // Архив (снят с продажи)
}
